const { buildProfilV2Slash } = require('./profil-v2-factory');

module.exports = buildProfilV2Slash(
    'profil',
    'Affiche ta fiche membre BLZ : stats, guilde, badges, quêtes, trophées et inventaire.',
    'profil'
);
