const { buildProfilV2Slash } = require('./profil-v2-factory');

module.exports = buildProfilV2Slash(
    'profil',
    'Affiche la fiche profil BLZ : progression, guilde, badges et raccourcis quêtes, trophées, inventaire.',
    'profil'
);
