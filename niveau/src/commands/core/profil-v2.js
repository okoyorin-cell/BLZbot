const { buildProfilV2Slash } = require('./profil-v2-factory');

module.exports = buildProfilV2Slash(
    'profil-v2',
    'Profil BLZ (carte 1024×381) : stats, guilde, badges exclusifs, titre staff.',
    'profil-v2'
);
