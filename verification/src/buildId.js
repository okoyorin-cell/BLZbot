/**
 * Identifiant de build (incrémenter quand l’UI côté membre change).
 * Sert à vérifier que Pebble exécute bien le dernier code : GET /health → verif_ui
 */
module.exports = { VERIF_BUILD_ID: 'verif-ui-linkbtn-20260504' };
